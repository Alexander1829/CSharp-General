            catch (Exception exc)
            {
                LogHelper.AddError(LogPriorities.Normal, "Не удалось получить список заявок на конкурсы для членов конкурсной комиссии", exc.ToString()); //Log in logger, but throw later
throw;
            }